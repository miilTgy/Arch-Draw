# Arch Draw

Arch Draw 是一个用于绘制可视化计算机体系结构图的绘图软件，目标是让硬件架构设计像使用 draw.io 一样直观。

它面向计算机体系结构和硬件设计场景，支持：

- 自动处理连线布局，尽量避免连线重叠。
- 将一组连线指定为同一条 datapath。
- 在任意一根连线或一组 datapath 上插入寄存器。
- 根据架构图生成 Chisel RTL。

项目仍在开发中，后续会继续完善绘图交互、datapath 编辑和 RTL 生成能力。
